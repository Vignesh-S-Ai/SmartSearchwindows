# Requirements for AI-powered local file search engine
# Install with: pip install -r requirements.txt

# Core dependencies
faiss-cpu==1.7.4  # FAISS vector search (CPU version for Windows)
numpy==1.26.3  # Required by FAISS

# API and web server
fastapi==0.109.0  # FastAPI web framework
uvicorn[standard]==0.27.0  # ASGI server
pydantic==2.5.3  # Data validation
python-multipart==0.0.6  # Form data parsing

# Google Gemini API
google-generativeai==0.8.3  # Gemini API client (use this, not google-genai)

# Text processing and search
rapidfuzz==3.6.1  # Fuzzy string matching
rank-bm25==0.2.2  # BM25 algorithm

# File monitoring
watchdog==3.0.0  # Cross-platform file system events (replaces FSEvents)

# Document parsing
PyPDF2==3.0.1  # PDF parsing
python-docx==1.1.0  # DOCX parsing
python-pptx==0.6.23  # PowerPoint parsing
openpyxl==3.1.2  # Excel parsing
Pillow==10.2.0  # Image processing

# Environment configuration
python-dotenv==1.0.0  # .env file loading

# Electron frontend (run separately)
# electron==28.0.0  # Install via npm in ui/ directory
